[pytest]
# webob (used by webtest) still imports the deprecated `cgi` module on
# Python 3.12; it can't be fixed from this project, so silence it.
filterwarnings =
    ignore:'cgi' is deprecated and slated for removal:DeprecationWarning:webob\.compat
